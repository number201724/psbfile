#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>
#include "compress.h"
#include <iostream>
#include <fstream>
#include <string>

using namespace std;


int main (int argc, char* argv[])
{
	/*
	fstream input("ch_azusa", ios::binary | ios::in );
	uint32_t pos;
	char* data;
	uint32_t outsize;
	unsigned char* compr;
	

	input.seekg(0, ios::end);
	pos = input.tellg();
	input.seekg(0, ios::beg);
	data = new char[pos];
	input.read(data, pos);
	
	compr = lzss_compress((const unsigned char*)data,pos,4,&outsize);
	*/
	return 0;
}

